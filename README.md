# Tahwul Dashboard – Frontend Assessment

This project is built using React + TypeScript (Vite) as part of the technical assessment.

## Tech Stack
- React
- TypeScript
- Vite
- CSS Modules
- Recharts

## How to run

npm install
npm run dev

## Architecture

The project is built using reusable component architecture instead of page-level JSX to simulate a scalable enterprise dashboard.

## Assumptions
- All data is mocked using static JSON
- Focus was on UI architecture and component reusability

## Improvements with more time
- Add responsiveness refinements
- Add animations
- Improve accessibility
